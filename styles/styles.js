const borderRadius = (level) => {
	return `border-radius: ${level * 5}px;`;
};

const modalCard = {
	modalBody: '#6BABAA',
	button: '#95D3BE',
	color: '#FEFEFE',
};

const styles = {
	borderRadius,
	modalCard,
};

export default styles;
